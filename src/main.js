import React from "react";
import ReactDOM from "react-dom/client";
import VPLEditor from "./VPLEditor";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <VPLEditor />
  </React.StrictMode>
);
